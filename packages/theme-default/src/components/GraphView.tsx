/**
 * GraphView — Re-export shim for backward compatibility.
 *
 * The actual implementation lives in ./graph/index.tsx.
 */

export { default } from './graph/index';
export type { Props } from './graph/types';
