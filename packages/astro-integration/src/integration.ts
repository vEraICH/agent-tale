// @agent-tale/astro-integration
// Wires the Content Graph Engine into Astro's build pipeline.

export default function agentTaleIntegration() {
  return {
    name: 'agent-tale',
    hooks: {},
  };
}
