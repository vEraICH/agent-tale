[build]
buildCommand = "pnpm install --ignore-scripts && pnpm turbo run build --filter=@agent-tale/vra-lab..."

[deploy]
startCommand = "node sites/vra-lab/dist/server/entry.mjs"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
